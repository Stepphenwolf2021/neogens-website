# Archive — previous site (neo-generalist studio positioning)

These pages are the site as it stood before the Modern Knowledge Management relaunch.
Kept for reference only: they are `noindex`, not linked from the live navigation, and
not listed in `sitemap.xml`.

`style.css`, `script.js` and `hero-*.png` are used only by these archived pages.
