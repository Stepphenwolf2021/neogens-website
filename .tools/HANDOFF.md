# HANDOFF — neogens-website · 2026-08-15

## Goal

จัดโครงเนื้อหา neogens.co ใหม่เป็นสามภาคที่ไม่ปนกัน — ภาค 1 แนวคิดที่ใช้ได้กับทุกองค์กร · ภาค 2 โซลูชันสำหรับพิพิธภัณฑ์และห้องสมุด · ภาค 3 โครงการเพื่อสาธารณะ MKM for Coffee
พร้อมแก้สองปัญหาที่พบจากการรีวิวในบท "ผู้บริหารพิพิธภัณฑ์" คือ ของดีที่สุดอยู่ลึกเกินไป และ ontology / knowledge graph ถูกใช้หลายสิบครั้งโดยไม่เคยอธิบาย

---

## Current State

**ยังไม่ commit และยังไม่ publish อะไรทั้งสิ้น** commit ล่าสุดคือ `05860f7` ซึ่งเป็นของก่อนหน้าเซสชันนี้

### สร้างใหม่ — เว็บ

| ไฟล์ | สถานะ |
|---|---|
| `coffee.html` · `th-coffee.html` | บทความ MKM for Coffee เต็มหน้า ในธีมเว็บ · แผนที่ Coffee Belt ก่อนบทความ · สารบัญ 10 หัวข้อ · การ์ด 14 ใบ · ฟอร์มสมัครท้ายหน้า · ตรวจแล้วเนื้อหา 108/108 ชิ้นตรงไฟล์ต้นฉบับที่ Noppadol ส่งมา |
| `problem.html` · `th-problem.html` | **เขียนใหม่ทั้งหน้า** เป็นภาค 1 ฉบับทั่วไป (Claude เขียน ตามที่อนุมัติ) |
| `layer.html` · `th-layer.html` | ใหม่ · ontology กับ knowledge graph คืออะไร · 4 บล็อก |
| `advantage.html` · `th-advantage.html` | เนื้อหาเดิมของ `problem.html` ย้ายมาทั้งหน้า **คำของ Noppadol ครบทุกตัวอักษร** เปลี่ยนเฉพาะป้ายหัวข้อ |
| `sovereignty.html` · `th-sovereignty.html` | ใหม่ · AI Sovereignty · 3 บล็อก |

### แก้ไข — เว็บ

- **nav / drawer / footer ทั้ง 35 ไฟล์** จัดกลุ่มใหม่เป็นสามภาค · top nav เหลือ 4 รายการ · จุดยุบเมนูเลื่อนจาก 960px เป็น 1120px
- `sitemap.xml` เพิ่ม 8 URL ใหม่
- ยืนยันด้วยการ diff ข้อความที่มองเห็นแล้วว่า **ขั้นจัดเมนูไม่แตะเนื้อหาในตัวหน้าเลยสักไฟล์**

### แก้ไข — Cloudflare Worker (repo แยก)

`~/projects/neogens-briefing-worker/src/index.js`

- รับฟิลด์ใหม่ `topic` (รับเฉพาะค่าใน allow-list `MKM Coffee`) และ `originCountry` (คนละตัวกับ `country` ที่มาจาก CF-IPCountry)
- subject เป็น `MKM Coffee — <org>` เมื่อมี topic มิฉะนั้นคงเดิม
- auto-reply แยกฉบับกาแฟ EN/TH
- `RATE_LIMIT_PER_HOUR` **5 → 20** แก้เมื่อ 2026-08-15 หลังฟอร์มยิงไม่ผ่านจริงบนเว็บ สาเหตุคือ curl ตอน deploy กับเบราว์เซอร์ใช้ IP สาธารณะเดียวกัน โควตา 5 หมดเร็วเกินไป
- **มีเทสต์ `test_worker.mjs` 31 ข้อ ผ่านหมด** รวม 8 ข้อที่พิสูจน์ว่าเส้นทางเดิมของพิพิธภัณฑ์ไม่เปลี่ยน
- ไฟล์สำรองก่อนแก้ `src/index.js.bak-2026-08-15`
- **deploy ไปแล้วหนึ่งรอบ version `09b6f048` แต่รอบที่แก้ rate limit ยังไม่ได้ deploy**
- หน้ากาแฟแยกข้อความ error กรณี 429 ออกจากกรณีล้มเหลวจริงแล้ว ทั้ง EN และ TH — **ฟอร์มเดิมอีก 26 หน้ายังไม่ได้แยก** ถ้าโดน 429 จะขึ้นว่าส่งไม่สำเร็จเฉย ๆ ซึ่งบอกสาเหตุไม่ตรง เป็นงานที่ควรทำต่อ

### สถานะการตรวจ

`python3 .tools/check.py` → 37 ไฟล์ · ไม่มี error · เตือน 24 ข้อ ทั้งหมดเป็นตัวเลขในบทความกาแฟที่มีแหล่งอ้างอิงกำกับ **ไม่ต้องแก้**

---

## Decisions made — ห้ามรื้อใหม่

1. โครงเว็บเป็นสามภาค ไม่ปนกัน เชื่อมโยงกันได้
2. MKM for Coffee เป็น practice area ที่สอง หน้าตากลืนธีมเว็บ มีหน้า TH แยกจริง
3. **ไม่ใส่ราคาหรือช่วงราคาบนเว็บ** เพราะเป็น consulting service
4. หน้า `problem.html` เขียนใหม่เป็นฉบับทั่วไป ของเดิมย้ายไป `advantage.html`
5. copy ใหม่ Claude เขียนแล้วขึ้นได้เลย ไม่ต้องรออนุมัติทีละชิ้น
6. PDPA ไม่ลงรายละเอียดกฎหมาย ยืนยันแค่หลัก AI Sovereignty
7. แผนที่ Coffee Belt ไม่มาร์กประเทศ มีแค่แถบเขตร้อน
8. deploy Worker ผ่าน `deploy.command` ไม่ใช่ให้ Claude รัน wrangler เอง
9. **ความเห็นรีวิวชุดที่สอง (ที่พูดถึง Data Science / AI Transformation / ทีมงานรุ่นใหม่) ไม่ได้อ่านเว็บจริง ตรวจแล้วคำเหล่านั้นอยู่ในเว็บ 0 ไฟล์ ห้ามเอาไปแก้ตาม** ที่ใช้ได้จากรีวิวนั้นมีข้อเดียวคือเรื่องธรรมาภิบาลข้อมูล ซึ่งทำไปแล้วในหน้า sovereignty

---

## Open questions — Noppadol ยังไม่ตอบ

1. auto-reply เดิมในหน้า `services` / `engagement` / `honest` และใน Worker ใช้คำว่า **ครับ** อยู่หลายจุด แต่สกิล `neogens-thai-voice` ห้ามใช้ในเนื้อความ ฉบับกาแฟที่เขียนใหม่จึงไม่ใช้ ตอนนี้สองโทนไม่ตรงกัน จะปรับให้ตรงกันไหม
2. `.tools/add_coffee_nav.py` เป็นสคริปต์ใช้ครั้งเดียวจบแล้ว จะเก็บไว้ในรีโปหรือลบ
3. หน้าแรกยังมีย่อหน้าที่เขียนว่า *"We start with museums, libraries and archives… Other practice areas follow."* ยังไม่ได้ปรับให้พูดถึงกาแฟ จะปรับหรือคงไว้
4. footer ใช้ `hello@neogens.co` แต่ commit `37c98b8` เคยเปลี่ยนเป็น `noppadol@neogens.co` อันไหนตั้งใจ

---

## Constraints

**Hard**

- **อ่าน `.tools/LESSONS.md` ให้จบก่อนพิมพ์ตัวอักษรแรกของเซสชัน** แล้วขึ้นต้นข้อความแรกด้วย `อ่าน .tools/LESSONS.md แล้ว — N ข้อ` ตอนนี้ N คือ 9
- **ข้อความที่ Noppadol เขียนมาเอง ห้ามแก้แม้แต่ตัวเดียว** เห็นอะไรที่คิดว่าควรแก้ ให้ถามก่อนแล้วรอคำตอบ
- ใช้ `git --no-optional-locks` กับ `status` และ `diff` เสมอ และเช็กว่า `.git/index.lock` ไม่ค้างก่อนบอกว่างานเสร็จ
- Claude แก้ไฟล์อย่างเดียว ไม่ commit ไม่ push ไม่ deploy เอง
- ไม่มี build step · static HTML ไฟล์เดียวจบต่อหนึ่งหน้า · CSS กับ JS อยู่ในไฟล์เดียวกัน
- แก้ copy ที่ไหน ต้องแก้อีกภาษาให้ตรงกัน และเช็ก `<title>` `meta description` `og:description` `alt` `aria-label` `placeholder`
- ห้าม letter-spacing ติดลบในกฎ CSS ที่มีข้อความไทย · line-height ไทยอยู่ที่ 1.75–1.9
- ห้ามใส่ตัวเลขสถานะระบบที่ยืนยันไม่ได้ · ห้ามใส่ชื่อฐานข้อมูล เฟรมเวิร์ก หรือสถาปัตยกรรมภายในบนเว็บ

**Soft**

- ภาษาไทยของ Neo Gens ใช้สกิล `neogens-thai-voice` เมื่อ Claude เป็นคนเขียน · ห้ามลงท้าย ครับ ในเนื้อความ · เขียนจากความคิด ไม่แปลจากอังกฤษ
- เนื้อหาใหม่ให้สั้น กระชับ เข้าประเด็น
- แก้ทั้งไซต์ด้วยสคริปต์ที่เก็บไว้ใน `.tools/` แล้วตรวจด้วย diff ระดับข้อความที่มองเห็น อย่าแก้ทีละไฟล์ด้วยมือ
- ประหยัด context — กรองผลลัพธ์ก่อนดึงเข้ามา อย่าเปิดไฟล์ HTML ทั้งไฟล์ (ไฟล์ละ 50–90KB)

---

## Next step

ย้ายบล็อกตัวอย่าง AR ดาราศาสตร์ (`สามคน วัตถุชิ้นเดียวกัน คนละความลึก` · 2/5/14 เส้น · ราหูอมจันทร์ · ภาพ ก่อน/หลังมี ontology) ออกจาก `about.html` และ `th-about.html` ไปต่อท้ายบล็อก `วัตถุชิ้นเดียวกัน เย็นวันเดียวกัน ผู้ชมสามคน` ใน `experience.html` และ `th-experience.html` — **ย้ายบล็อกอย่างเดียว ห้ามแก้คำ** เพราะเป็นข้อความของ Noppadol

จากนั้นอีกสองงานที่ค้าง

1. เติมเนื้อหาใต้หัวข้อ `กายวิภาค — สามส่วน และคุณคิดแบบนี้อยู่แล้ว` ในหน้า `what.html` / `th-what.html` ที่ตอนนี้มีแต่หัวข้อไม่มีเนื้อ (Claude เขียนได้ ตามข้อ 5 ใน Decisions)
2. ปรับหน้าแรกจากสารบัญเรียงยาว เป็นสามประตูแยกภาคชัดเจน

---

## Context files — อ่านตามลำดับนี้

1. `.tools/LESSONS.md` — บทเรียน 9 ข้อ **บังคับอ่านก่อนพิมพ์อะไรทั้งสิ้น**
2. `CLAUDE.md` — กติกาโปรเจกต์
3. `.tools/HANDOFF.md` — ไฟล์นี้
4. `.tools/check.py` — ตัวตรวจก่อน publish รู้ว่ามันตรวจอะไรก่อนแก้อะไร
5. `.tools/regroup_nav.py` — โครงเมนูสามภาคที่ใช้อยู่จริง แก้ที่นี่แล้วรันใหม่ อย่าแก้ HTML ทีละไฟล์
6. `.tools/build_sections.py` — เครื่องมือสร้างหน้าใหม่จากแม่แบบ `why.html`
7. `th-experience.html` และ `th-about.html` — สองไฟล์ของ next step ใช้ `grep`/`sed` ดึงเฉพาะช่วงที่ต้องการ อย่าเปิดทั้งไฟล์
8. `.tools/build_coffee.py` + `.tools/make_coffee_belt.py` — อ่านเฉพาะเมื่อจะแก้หน้ากาแฟหรือแผนที่
9. `~/projects/neogens-briefing-worker/src/index.js` + `test_worker.mjs` — อ่านเฉพาะเมื่อจะแตะฟอร์มหรือ Worker

---

## สั่งงานจากมือถือระหว่างเดินทาง

`remote.command` ที่รากรีโป — ดับเบิลคลิกแล้วปล่อยหน้าต่างเปิดค้างไว้ เซสชันรันบนเครื่องนี้
มือถือเป็นแค่หน้าต่างเข้ามาสั่ง เปิดแอป Claude แตะแท็บ Code แล้วเลือก **Neo Gens website**

- ใช้โหมด interactive ไม่ใช่ server mode เพราะ server mode เลิกเองหลังเน็ตหลุดราวสิบนาที
- มี `caffeinate` กันเครื่องหลับ และ loop เปิดใหม่ให้เองถ้าโปรแกรมหลุด
- **แจ้งเตือนบนมือถือต้องเปิดตามลำดับ** เปิดเซสชัน แล้วเชื่อมมือถือให้ติดก่อน จากนั้นค่อยพิมพ์ `/config` บนเครื่อง สองตัวเลือก `Push when Claude decides` กับ `Push when actions required` จะโผล่ก็ต่อเมื่อ Remote Control ทำงานและมือถือลงทะเบียนแล้ว ถ้าขึ้น `No mobile registered` ให้เปิดแอปบนมือถือค้างไว้แล้วรอเชื่อมรอบใหม่
- ปิดหน้าต่าง Terminal เมื่อไร เซสชันออฟไลน์ทันที
- Claude ยังไม่ commit ไม่ push ตามกติกาเดิม ถ้าต้องการให้ deploy แทนระหว่างเดินทาง ต้องสั่งให้ชัดในเซสชันนั้น

---

## สิ่งที่ Noppadol ต้องกดเอง — ยังไม่ได้ทำ

1. `~/projects/neogens-briefing-worker/deploy.command` — deploy Worker **ต้องทำก่อน** ไม่งั้นคนกรอกฟอร์มกาแฟจะได้ auto-reply ฉบับพิพิธภัณฑ์
2. `check.command` แล้ว `publish.command` ที่ `~/projects/neogens-website`
3. เปิด `coffee.html` `th-coffee.html` `th-layer.html` `th-problem.html` `th-sovereignty.html` ดูด้วยตา ทั้งธีมมืดและสว่าง สองความกว้าง — **Claude เรนเดอร์หน้าเว็บดูเองไม่ได้ในสภาพแวดล้อมนี้ ยืนยันได้แค่โครงสร้างกับเนื้อหา ยืนยันไม่ได้ว่าหน้าตาสวย**
4. **ฟอร์มกาแฟทดสอบได้เฉพาะที่ `https://www.neogens.co/th-coffee.html` เท่านั้น** Worker ยอมรับเฉพาะ Origin `neogens.co` และ `www.neogens.co` เปิดไฟล์ด้วย `file://` จะได้ CORS error เสมอ — เคยเสียเวลาไล่หาสาเหตุผิดมาแล้วหนึ่งรอบ ดูข้อ 7 ใน LESSONS
