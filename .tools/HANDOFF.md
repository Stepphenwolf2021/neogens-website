# HANDOFF — neogens-website · 2026-08-31

## เริ่มตรงนี้ — exec summary ขึ้นเว็บแล้ว สองภาษา

**หน้าใหม่สองหน้า** `exec-summary-museums.html` · `th-exec-summary-museums.html`
อยู่ใต้กลุ่ม **MKM for Museums & Libraries** ตามที่ Noppadol สั่งเมื่อ 08-31
เดิมคุยกันว่าจะทำเมนูใหม่ชื่อ *MKM Insights* แล้วเปลี่ยนใจ **ยังไม่ทำเมนูใหม่**

```
ต้นฉบับ      .tools/exec-summary-source/neogens-exec-summary-museums-EN.md · -TH.md
สร้างหน้า     .tools/build_exec_summary.py     แม่แบบคือ coffee-farmer ทั้งสองภาษา
เติมสามย่อหน้า .tools/build_exec_summary_src.py  แทรกท้ายหัวข้อ 2 ของต้นฉบับ
ตรวจ         .tools/verify_exec_summary.py    เทียบตัวอักษรกับต้นฉบับ
             .tools/verify_exec_summary.mjs   วัด getComputedStyle ด้วย jsdom
```

**แก้เนื้อหาที่ `.tools/exec-summary-source/` แล้วรัน `build_exec_summary.py` ใหม่**
สคริปต์เรียก `sync_nav.py` → `build_jsonld.py` → `add_breadcrumbs.py` ต่อให้เอง
หลังจากนั้นต้องรัน `build_footer.py` เองอีกที ถ้าแตะรายการใน footer

**ฟอร์มของแม่แบบถูกถอดออกโดยตั้งใจ** ฟอร์มนั้นส่ง `topic:'MKM Coffee'` ซึ่ง Worker
มี allow-list รับแค่ค่านี้ค่าเดียว ผู้อำนวยการพิพิธภัณฑ์ที่กรอกจะถูกบันทึกเป็นสายกาแฟ
ปิดท้ายหน้าด้วยปุ่มไปหน้า `contact.html` แทน **ถ้าอยากได้ฟอร์มจริงบนหน้านี้
ต้องเพิ่มค่า topic ใหม่ที่ฝั่ง worker ก่อน** แล้วค่อยกลับมาแก้หน้า

**สามย่อหน้าใหม่ท้ายหัวข้อ 2** สามทางที่ผู้บริหารมักเลือก มาตรฐาน · เครื่องมือ · จัดการเอง
และทำไมทั้งสามทางยังไม่พอ ปิดด้วยการส่งไม้ต่อหัวข้อ 5 **ฉบับไทยเขียนขึ้นใหม่ ไม่ได้แปล**

**ค้างสองเรื่อง**

```
1  Noppadol เปิดหน้าจริงดูด้วยตา สองธีม สองความกว้าง  ผมเรนเดอร์ดูเองไม่ได้
2  check.py เตือนเพิ่ม 10 ข้อ เป็นตัวเลข 46% 11% 8% 70% 43% ที่มีแหล่งอ้างอิงกำกับ
   ชนิดเดียวกับที่บทความกาแฟเตือนอยู่แล้ว รวมเป็น 34 ข้อ  ไม่มี error
```

```
โครง 6 หัวข้อ   วงการถามแล้วแต่ยังไม่มีคำตอบ (UNESCO/ICOM · 46% ใช้ AI แล้ว 8% มี charter · AAM 70%)
                ปัญหามีชื่อทางการแล้ว (LOC C2PA for G+LAM · OCLC สามโหมดที่พัง)
                ตัวอย่างที่พังจริง (Nasher · Mariners' Museum)
                บทเรียนที่เยลจ่ายไปแล้ว (LUX 41 ล้านระเบียน)
                ข้อเสนอบนหลักห้าข้อของ MKM
                สิ่งที่เราไม่กล้าอ้าง
```

**ฉบับไทยเขียนขึ้นใหม่ ไม่ได้แปลจากอังกฤษ** ชื่อเรื่องคนละตัว หัวข้อเป็นภาพไม่ใช่หลักการ
และมีประโยคที่ฉบับอังกฤษไม่มี ท้ายหัวข้อ 5 เรื่อง ontology เกิดจากการนั่งคุยกับภัณฑารักษ์
**ห้ามไปไล่แก้ให้สองฉบับตรงกัน** ความต่างเป็นเจตนา ใช้ Sarabun line-height 1.76 จัดชิดซ้าย

**ค้างอยู่ตรงคำถามเดียว รอ Noppadol ตอบ** จะย่อคำอธิบายเรื่อง *ไม่มีมาตรฐาน เครื่องมือ หรือองค์กรใด
แก้ได้ตามลำพัง* (สามทางออกที่ผู้บริหารมักใช้ และทำไมไม่พอทั้งสามทาง) ลงเป็นสามย่อหน้า
แล้วใส่ต่อท้ายหัวข้อ 2 ทั้งสองฉบับไหม — จะทำให้หัวข้อ 2 ส่งไม้ต่อหัวข้อ 5 ได้แน่นกว่าเดิม

> **ยังไม่ได้ตัดสินว่าเอกสารสองฉบับนี้จะเก็บไว้ที่ไหน** ไม่ได้เอาเข้ารีโปเว็บโดยตั้งใจ
> เพราะยังไม่ได้ถาม ถ้าจะเก็บในรีโปนี้ ที่ปลอดภัยคือใต้ `.tools/` ซึ่งไม่ถูกเสิร์ฟ
> **ถามก่อนย้าย** อย่าตัดสินใจแทน

**ฝั่งเว็บไม่ค้างแล้ว** `.tools/publish.sh` ที่ค้างมาตั้งแต่ 08-25 ขึ้นไปกับ `dfe02cb` แล้ว
เนื้อการแก้คือย้าย `trap 'git reset'` ไปตั้ง **ก่อน** `git add -A` กันกรณีปิดหน้าต่างตอนอ่านรายการ
ขั้น 3 แล้วไฟล์ค้าง stage ทั้งหมด (เกิดจริงกับ personal-library 08-25) และปลด trap ทันทีหลัง
commit ผ่าน ไม่งั้นตอนจบสคริปต์จะไป reset ทับงานที่เพิ่ง commit

**HEAD `dfe02cb` (08-29 23:43) เท่ากับ `origin/main`** · ค้างเฉพาะ `LESSONS.md` กับไฟล์นี้ของรอบ 08-30

---

## สถานะเมื่อ 2026-08-23

**งานของวันที่ 08-23 ขึ้นเว็บครบแล้ว** HEAD `0294984` เท่ากับ `origin/main`
ยืนยันจากการดึงหน้าจริงมาดูแล้ว ไม่ใช่แค่ดูว่า push ผ่าน

**ตอนนั้นค้างสี่ไฟล์ ตอนนี้เหลือไฟล์เดียว** สามไฟล์ขึ้นไปกับ `b72f90a` เมื่อ 08-25 เรียบร้อยแล้ว
คือ `HANDOFF.md` · `CLAUDE.md` · `.tools/publish.conf` เหลือแต่ `.tools/publish.sh` ดูหัวข้อบนสุด

### งานถัดไป เรียงตามที่ควรทำ

```
1  ตอบคำถามเรื่องย่อสามย่อหน้าเข้าหัวข้อ 2 ของ exec summary  ดูหัวข้อบนสุด
2  ขยาย publish-kit ไปโปรเจกต์อื่น  ดู ~/projects/publish-kit/ROLLOUT.md
   เริ่มที่ neogens-briefing-worker  มีขั้นละเอียดอยู่ในหัวข้อ "ลงมือครั้งแรก" ของไฟล์นั้น
3  deploy worker รอบ RATE_LIMIT_PER_HOUR 5 → 20  ถ้ายืนยันแล้วว่ายังไม่ได้ deploy
```

**ปิดไปแล้วสามข้อเมื่อ 08-24**

```
footer ห้าคอลัมน์      Noppadol ดูของจริงแล้ว OK  ไม่ต้องขยับอะไรอีก
eyebrow สองหน้า        coffee-farmer กับ coffee-demo คงคำเดิมไว้ ไม่เปลี่ยน
                      Join the first group · Build the real one  ห้ามไปแก้ให้เหมือนหน้าอื่น
.tools/ ถูกเสิร์ฟไหม   ตรวจแล้ว ไม่ถูกเสิร์ฟ  ดูหัวข้อข้างล่าง
```

**สิ่งที่ทำเสร็จและยืนยันแล้วเมื่อ 08-23** — ตัดคำว่า ครับ ในเนื้อความ 10 จุด ·
เติม (อังกฤษ) ให้ลิงก์ long-read ใน footer · หัวกลุ่มบนแถบเมนูเข้มทั้งภาค ·
`aria-current` เหลืออันเดียวต่อหน้า · ช่องใหม่ในฟอร์มกาแฟ · เอาแถบ GET IN TOUCH ออก ·
footer ห้าคอลัมน์ · เมลยืนยันทวนข้อมูลที่กรอก · ปลด `if (notified)` · รัด regex อีเมล ·
ย้ายมาใช้ publish-kit

**ตรวจสามข้อบนของจริงผ่านหมดแล้ว** เมลแจ้งเตือนเข้า hello@ · เมลยืนยันถึงผู้กรอก
พร้อมบล็อกทวนข้อมูล · `a@b..com` ถูกบล็อกตั้งแต่หน้าเว็บ

### แก้เพิ่มเมื่อ 08-23 รอบบ่าย — ตัวบ่งชี้หน้าปัจจุบันบนแถบเมนู

Noppadol ส่งภาพหน้าจอมาว่าเมนู `01 · MKM for Coffee` ค้างเป็นสีเขียว ตรวจแล้วสีนั้นถูก
เพราะภาพถ่ายจากหน้า 01 พอดี แต่ไล่ต่อจนเจอของจริงสองอย่าง ดูข้อ 12 ของ `LESSONS.md`

```
หัวกลุ่มบนแถบเมนู   ได้ class=on เฉพาะหน้าแรกของกลุ่ม  →  ตอนนี้ติดทุกหน้าในภาค
aria-current=page   5–6 อันต่อหน้า                      →  ตอนนี้เหลืออันเดียวในเมนู
```

แก้ที่ `.tools/sync_nav.py` ที่เดียว แล้วซิงก์ลง 47 หน้า **ไม่ได้แตะ CSS สักบรรทัด**
เพราะหัวกลุ่มใช้ `class="on"` ตัวเดิมที่มีกฎ `.nav-links a.on{color:var(--fg)}` อยู่แล้ว
`aria-current` ในแถบเส้นทาง `<li>` ยังอยู่ตามแบบ WAI-ARIA breadcrumb หน้าละ 1 อัน
ถ้าอยากให้เหลืออันเดียวจริง ๆ ทั้งหน้า ต้องสั่ง แล้วผมถอดอันในแถบเส้นทางออกให้

ด่านตรวจใน `sync_nav.py` เพิ่มสามข้อ และทดสอบแล้วว่าจับได้จริงทั้งสามแบบ
คือ *มี* · *ชี้ถูก* · **ไม่เกินหนึ่ง** กับ *หัวกลุ่มติดเท่ากันทุกหน้าในกลุ่ม*

*(สองรอบข้างบนขึ้นเว็บแล้วที่ `c74a57a` กับ `d8b4260`)*

### แก้เพิ่มเมื่อ 08-23 รอบเย็น — ฟอร์มหน้าภาคกาแฟ · `.tools/add_interest_field.py`

Noppadol สั่งให้หน้าในภาคกาแฟเหลือทางติดต่อทางเดียวคือฟอร์ม และเพิ่มช่องถามว่าอยากรู้อะไร

```
เพิ่ม   textarea id="interest" ท้ายฟอร์ม  ไม่บังคับ  maxlength 600
        EN  What you would like to learn from the vault
        TH  ข้อมูลที่คุณอยากรู้จาก Coffee Knowledge Vault   ← คำของ Noppadol เอง ห้ามเกลา
เอาออก  <section class="cta"> แถบ GET IN TOUCH   ทั้งสิบหน้า
```

หน้าที่แตะ 10 หน้า คือ `mkm-for-coffee` · `-why-now` · `-commons` · `coffee-farmer` · `coffee-demo` ทั้งสองภาษา

> **`privacy.html` กับ `seo-as-knowledge-management.html` ใช้ฟอร์มหน้าตาเดียวกัน**
> เพราะสร้างจากแม่แบบ `coffee-farmer.html` แต่ไม่ได้อยู่ในภาคกาแฟ **จึงไม่ได้แตะ**
> สองหน้านั้นยังมีแถบ GET IN TOUCH และยังไม่มีช่อง interest ถ้าอยากให้เหมือนกันต้องสั่ง

**ต้อง deploy สองที่ ไม่ใช่ที่เดียว** — worker คัดฟิลด์ทีละตัว ไม่ได้ส่งต่อทั้งก้อน
ถ้าขึ้นแต่เว็บ ฟอร์มจะขึ้นว่า *Received* ตามปกติ แต่ข้อความที่คนกรอกหายเงียบ ๆ

```
~/projects/neogens-website          check.command → publish.command
~/projects/neogens-briefing-worker  deploy.command      ← ห้ามลืมอันนี้
```

ฝั่ง worker เพิ่ม `cleanBlock()` แยกจาก `clean()` เพราะ `clean()` ยุบบรรทัดทิ้งเพื่อกัน
header injection ซึ่งถูกสำหรับค่าที่ไปต่อในหัวเมล แต่ผิดสำหรับกล่องข้อความ
`cleanBlock()` เก็บ `\n` ไว้ แปลง `\r` ทิ้ง และใช้ได้เฉพาะกับค่าที่ไปอยู่ในเนื้อเมลเท่านั้น
`test_worker.mjs` 45 ข้อผ่านหมด ในนั้นเป็นข้อของช่องใหม่ 9 ข้อ

วัดด้วย jsdom ที่โหลด CSS ฝังในหน้าเอง — `display:block` · `min-height:98px` · `line-height:1.75`
· `border-radius:18px` · `resize:vertical` ทุกหน้า ไม่ใช่ค่า default ของเบราว์เซอร์

### แก้เพิ่มเมื่อ 08-23 รอบดึก — footer ห้าคอลัมน์ · `.tools/build_footer.py`

Noppadol สั่ง "MKM for Coffee น่าจะอยู่แยกกับ Company" และให้ป้าย `Take part` เป็น `Start the conversation`

```
เดิม  4 คอลัมน์   คอลัมน์ 4 ชื่อ Part 3 & company  กาแฟปนกับบริษัท  long-read ก็ไปกองอยู่ตรงนั้น
ใหม่  5 คอลัมน์   แนวคิด · พิพิธภัณฑ์ (+long-read) · การทำงานร่วมกัน · ภาค 3 โครงการเพื่อสาธารณะ · บริษัท
```

**footer มีต้นฉบับเดียวต่อภาษาแล้ว** ปิดช่องว่างที่ฉบับเช้าบันทึกไว้ว่า *footer ไม่มีต้นฉบับ ต้องไล่ทีละหน้า*

> **แก้ไขเมื่อ 08-31 — บรรทัดเดิมบอกที่อยู่ต้นฉบับผิด** ฉบับ 08-23 เขียนว่าให้แก้ที่
> `.tools/shell/footer.en.html` · `footer.th.html` **ซึ่งไม่จริง สองไฟล์นั้นเป็นผลลัพธ์**
> `build_footer.py` เขียนทับทุกครั้งที่รัน ต้นฉบับจริงคือตัวแปร `COLS` ในตัวสคริปต์เอง
> แก้ผิดที่แล้วรัน จะได้บรรทัด *เขียน footer ใหม่ 0 หน้า · ไม่ผ่าน 0* ซึ่งหน้าตาเหมือนสำเร็จ
> ดูข้อ 15 ของ `LESSONS.md`  ส่วน `nav.en.html` · `nav.th.html` เป็นต้นฉบับจริง แก้ได้ตรง ๆ

เจอระหว่างทาง **คอลัมน์กาแฟเดิมขาดไปสองหน้า** ไม่มี `02 · Why this is possible now`
กับ `03 · How a commons stays a commons` เลยทั้งสองภาษา ตอนนี้ครบห้าหน้าตรงกับเมนู

**ป้าย eyebrow แก้แค่สามหน้า** คือ `mkm-for-coffee` · `-why-now` · `-commons` ที่เขียนว่า `Take part`
`coffee-farmer` เขียนว่า *Join the first group* และ `coffee-demo` เขียนว่า *Build the real one`
ทั้งสองเป็นคำเฉพาะหน้า ยังไม่ได้แตะ **รอ Noppadol บอกว่าจะให้เป็น Start the conversation ด้วยไหม**

**ฝั่งไทยคงป้ายเดิม** `ร่วมเป็นผู้สร้างคลังความรู้รุ่นแรก` ตามที่สั่ง
สองภาษาจึงไม่ตรงกันตรงจุดนี้โดยตั้งใจ ใครมาอ่านทีหลังอย่าเพิ่งไปแก้ให้ตรงกัน

> **เว็บนี้มี CSS สองรุ่นอยู่พร้อมกัน — เรื่องนี้ยังไม่ได้แก้**
> หน้าที่ลิงก์ `assets/site.*.css` เอากฎจากไฟล์ภายนอก และ `site.th.css` ยังตั้ง `.f-in{display:block}`
> ส่วนหน้าที่ฝัง CSS ทั้งก้อน (กาแฟ · privacy · seo) ใช้กฎรุ่นเก่ากว่า `repeat(4,1fr);gap:26px`
> กฎห้าคอลัมน์จึงเขียนให้เพียงพอในตัว มี `display:grid` ด้วย และวางท้ายสุดของ `<style>`
> ซึ่งอยู่หลัง `<link>` จึงชนะทั้งสองรุ่นด้วยลำดับ
> วัดด้วย jsdom ที่แทรก `site.*.css` เข้าไปเองแล้ว 46 หน้าผ่านหมด
> **แต่ความกว้างจริงของคอลัมน์ยังไม่มีใครเห็นด้วยตา** ห้าคอลัมน์ในกรอบ `1fr 2.4fr` ตกคอลัมน์ละราว 130px
> ป้ายยาวจะขึ้นบรรทัดใหม่ ถ้าดูแล้วอึดอัด บอกได้ ผมขยับ `.f-in` หรือลดเหลือสี่คอลัมน์ให้

*(สองรอบข้างบนขึ้นเว็บแล้วที่ `cf9b92e` กับ `36b813d`)*

### แก้เพิ่มเมื่อ 08-23 รอบดึกสอง — เมลยืนยันถึงผู้กรอก

Noppadol สั่งให้ "เพิ่ม Function ให้มี Email ตอบกลับไปที่คนกรอกแบบฟอร์ม"

**ของเดิมมีอยู่แล้ว** ขั้น 3 ของ `src/index.js` ส่งเมลตอบกลับหาผู้กรอกมาตั้งแต่ต้น
มีสี่สำนวนแยกตามหัวข้อและภาษา และ `replyTo` ตั้งเป็น `hello@neogens.co` ไว้ให้กดตอบได้ทันที
**ถ้าไม่เคยเห็นเมลนี้ ให้สงสัยว่ายังไม่ได้ deploy ก่อน อย่าเพิ่งไปเขียนใหม่**

สิ่งที่เพิ่มรอบนี้คือ **บล็อกทวนข้อมูลที่ผู้กรอกส่งมา** ต่อท้ายก่อนลายเซ็น

```
ข้อมูลที่เราได้รับ

ชื่อ · มะลิ
ฟาร์ม/โรงคั่ว · สหกรณ์ดอยช้าง
อีเมล · mali@doichang.co
ประเทศ · ไทย

สิ่งที่คุณอยากรู้จากคลัง
…
```

ช่องว่างไม่ขึ้นบรรทัด อีเมลขึ้นเสมอเพราะเป็นช่องบังคับช่องเดียว
ฟอร์มขอนัดหารือใช้ป้าย `Institution` / `หน่วยงาน` แทน ตามหลักเดียวกับ `notificationText()`
`test_worker.mjs` **60 ข้อผ่านหมด** เป็นข้อของบล็อกนี้ 15 ข้อ

ฝั่งเว็บแก้ข้อความในกล่อง `ok` ทั้ง 46 จุด ให้บอกว่ามีเมลยืนยันส่งไปแล้ว
**ไม่ได้แตะข้อความกรณี 202 ใน JS** เพราะกรณีนั้น worker ไม่ได้ส่งเมลตอบกลับจริง `replied=false`
ถ้าไปเขียนว่าส่งแล้วจะเป็นการโกหกผู้กรอก มีด่านตรวจกันไว้แล้วหนึ่งข้อ

### แก้เพิ่มเมื่อ 08-23 รอบดึกสาม — ปลด `if (notified)` และรัด regex อีเมล

**ขาตอบกลับไม่ผูกกับขาแจ้งเตือนอีกแล้ว** เกณฑ์ของขานี้คือ *เก็บข้อมูลสำเร็จ* ไม่ใช่ *เราได้รับแจ้งเตือน*
เดิมถ้าเมลเข้า `hello@` ล้ม ผู้กรอกไม่ได้อะไรเลย ทั้งที่ข้อมูลเขาถูกเก็บลง KV เรียบร้อยแล้ว
ตอนนี้ผลลัพธ์คืน `replyError` กลับมาด้วย ไม่กลืนเงียบ · ตรรกะสถานะ 200/202 คงเดิม

**regex ตรวจอีเมลรัดขึ้น ทั้งเว็บและ worker เป็นตัวเดียวกัน**

```js
/^[^@\s]{1,64}@(?:[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?\.)+[a-z]{2,24}$/i
```

เหตุผลไม่ใช่ความปลอดภัย แต่คืออัตรา bounce เมื่อเมลตอบกลับออกทุกครั้ง
ทุกการกดส่งจะยิงเมลไปยังที่อยู่ที่คนแปลกหน้าพิมพ์มา ที่อยู่ผิดบ่อย ๆ
จะไปลงที่ชื่อเสียงของโดเมน `send.neogens.co` ซึ่งกระทบเมลทุกฉบับที่ส่งจากที่นั่น

```
ตัดออก   a@b..com · a@b.c · a@-.- · a@.com · a@b-.com · a@-b.com
ยังผ่าน  a.b+tag@sub.example.co.th · x@xn--12c1bik6bbd.th · A@B.CO
```

> **ตัวนี้อยู่สองที่ ต้องแก้ให้ตรงกันเสมอ**
> เว็บ `.tools/set_email_regex.py` · worker ค่าคงที่ `EMAIL_RE` ใน `src/index.js`
> ถ้าเว็บหลวมกว่า ผู้ใช้จะกดส่งได้แล้วโดนตีกลับเป็น 400 ซึ่งบอกสาเหตุไม่ตรง
> ถ้าเว็บแน่นกว่า บางคนจะกรอกไม่ผ่านทั้งที่หลังบ้านรับได้
> ข้อแลกเปลี่ยนที่รู้ตัว โดเมนยูนิโค้ดดิบไม่ผ่าน ส่วน punycode `xn--` ผ่านปกติ
> และไม่มี regex ไหนจับ `mali@gmial.com` ได้ นั่นเป็นหน้าที่ของเมลยืนยัน

`test_worker.mjs` **68 ข้อผ่านหมด** เพิ่มรอบนี้ 8 ข้อ ในนั้นมีข้อที่จำลองให้เมลฉบับแรกล้ม
แล้วพิสูจน์ว่าฉบับที่สองยังออกถึงผู้กรอก และข้อที่ยิง 15 ที่อยู่ทดสอบผ่าน worker จริง

**`สวัสดีครับ` ใน `autoReplyText` ฉบับไทย — Noppadol สั่งให้คงไว้** อย่าไปแก้ให้ตรงกติกา

### ยืนยันของจริงบนเว็บแล้วเมื่อ 08-23 · HEAD `0294984`

ดึง `https://www.neogens.co/mkm-for-coffee.html` มาดูแล้ว ขึ้นครบทุกอย่าง
`Start the conversation` · ช่อง *What you would like to learn from the vault* ·
ข้อความ *A confirmation is on its way to your inbox* · ไม่มีแถบ GET IN TOUCH ·
footer ห้าคอลัมน์ `Part 3 · Public goods` แยกจาก `Company` และมีลิงก์กาแฟครบห้า

> **แต่ต้องต่อ `?cachebust=` ท้าย URL ถึงจะเห็น** ดึง URL เปล่า ๆ ได้ฉบับก่อน publish
> ตรงกับอาการที่บันทึกไว้เมื่อ 08-22 ว่ารากเว็บคืนหน้าเก่ามาให้
> **นี่ไม่ใช่บั๊กของเว็บ เป็นแคชของ GitHub Pages ซึ่งตั้ง `max-age` ไว้ราวสิบนาที**
> เวลาตรวจงานหลัง publish ให้ hard refresh หรือรอสิบนาที อย่าเพิ่งสรุปว่า deploy ไม่ขึ้น
> และอย่าเพิ่งไปไล่แก้โค้ด — เคยเสียเวลากับเรื่องนี้มาแล้วหนึ่งรอบ

### ย้ายมาใช้ publish-kit เมื่อ 08-23

`check.command` · `publish.command` · `.tools/publish.sh` เป็นฉบับกลางจาก `~/projects/publish-kit` แล้ว
ค่าที่ผูกกับโปรเจกต์นี้ย้ายไปอยู่ใน `.tools/publish.conf` ที่เดียว

**ห้ามแก้ `publish.sh` เฉพาะที่นี่** เจอเรื่องต้องแก้ ให้แก้ที่ต้นแบบแล้วกระจายกลับ

ของใหม่ต่างจากของเดิมสามอย่าง

```
เพิ่มขั้น 6  รัน DEPLOY_CMD ต่อหลัง push   ที่นี่เว้นว่าง เพราะ Pages ขึ้นเอง
เพิ่มด่าน    แยกสายกับ GitHub → หยุด ไม่ merge ให้
แก้รายการ   publish.sh เดิมบอกให้ไปดู km-for-museums-and-libraries.html เป็นหน้าจริง
            แต่มันเป็นทางเบี่ยงไปนานแล้ว รายการใน conf แก้ให้ตรงของจริงแล้ว
```

ทดสอบด้วยการ clone รีโปนี้ไปทั้งก้อนแล้วรันบนสำเนา **18 ข้อผ่านหมด**
ไม่ได้รันบนรีโปจริง เพราะไม่อยากให้ index ถูกแตะและเสี่ยง `index.lock` ค้าง ดูข้อ 6

> **แก้คำที่เคยเขียนไว้ผิดในฉบับ 08-23** — ตอนนั้นผมเขียนว่า `.tools/publish.conf`
> จะเปิดอ่านได้ที่ `neogens.co/.tools/publish.conf` **ซึ่งไม่จริง ตรวจของจริงแล้วเมื่อ 08-24**
>
> วิธีตรวจ ดึงสามที่มาเทียบกัน
> `robots.txt` คืนเนื้อหาพร้อมส่วนหัว `Content-Type` ส่วน `.tools/LESSONS.md`
> กับ `.tools/check.py` คืนผลหน้าตาเดียวกับ URL มั่ว ๆ ที่ไม่มีอยู่จริง คือไม่มีเนื้อหาเลย
>
> **สาเหตุคือรีโปนี้ไม่มีไฟล์ `.nojekyll`** GitHub Pages จึงยังประมวลผลด้วย Jekyll
> ซึ่งตัดโฟลเดอร์ที่ขึ้นต้นด้วยจุดออกจากเว็บที่สร้างเสมอ `.tools/` ทั้งโฟลเดอร์จึงไม่ถูกเสิร์ฟ
>
> **แต่เงื่อนไขนี้เปราะ** วันไหนมีคนเติม `.nojekyll` เข้ามา ซึ่งเป็นวิธีแก้ยอดนิยม
> เวลาต้องการโฟลเดอร์ที่ขึ้นต้นด้วยขีดล่าง `.tools/` ทั้ง 62 ไฟล์จะกลายเป็นสาธารณะทันที
> **ถ้าวันนั้นมาถึง ต้องกลับมาคิดเรื่องนี้ใหม่** และนั่นคือเหตุผลเดียวที่ยังควรเติม
> `Disallow: /.tools/` ใน `robots.txt` ไว้ล่วงหน้า — กันไว้เผื่อวันที่เงื่อนไขเปลี่ยน
>
> สรุปสำหรับตอนนี้ `.tools/publish.conf` ปล่อยขึ้นได้ ไม่มีใครอ่านได้จากอินเทอร์เน็ต

**ฝั่ง worker ยังยืนยันแทนไม่ได้** ต้องยิงคำขอจริงซึ่งจะสร้างเรกคอร์ดจริงและส่งเมลจริง
วิธีตรวจที่ถูกคือ Noppadol กรอกฟอร์มด้วยอีเมลตัวเองหนึ่งครั้งจากหน้าเว็บจริง แล้วดูสามอย่าง

```
1  hello@ ได้เมลแจ้งเตือน หัวเรื่อง  MKM Coffee — <ชื่อฟาร์ม>
2  อีเมลที่กรอก ได้เมลยืนยัน หัวเรื่อง  We received your details — Coffee Knowledge Vault
   ท้ายเมลต้องมีบล็อก What we have on record พร้อมสิ่งที่กรอกไว้ครบ
3  ลองกรอก a@b..com ดู  ต้องขึ้น error ใต้ช่องตั้งแต่ยังไม่ยิง  ถ้ายิงผ่านแปลว่าเว็บยังแคชอยู่
```

---

# บันทึกสถานะ

ไฟล์นี้บอกว่า **ตอนนี้เว็บเป็นอย่างไร** ไม่ใช่ว่าเคยตั้งใจจะทำอะไร
ทุกตัวเลขในนี้อ่านจากไฟล์จริงและ `git log` เมื่อ 2026-08-23 ไม่ได้คัดลอกจากฉบับก่อน

ฉบับก่อนหน้า (08-15) อ้างชื่อหน้าที่ถูก rename ไปหมดแล้ว และบอกว่า `LESSONS.md` มี 9 ข้อ
ถ้าเจอสำเนาเก่าที่ไหน ให้ทิ้ง อย่าเอามาตัดสินใจ

---

## เว็บนี้คืออะไร

`neogens.co` — เว็บของสำนักที่ปรึกษาด้าน Modern Knowledge Management
static HTML สองภาษา ไม่มี build step โฮสต์บน GitHub Pages มี CNAME

เนื้อหาแบ่งเป็นสามภาคที่ไม่ปนกัน

```
ภาค 1 · แนวคิด              ใช้ได้กับทุกองค์กร
ภาค 2 · พิพิธภัณฑ์และห้องสมุด  practice area แรก
ภาค 3 · โครงการเพื่อสาธารณะ   MKM for Coffee
```

---

## สถานะ ณ 2026-08-23

**Git** — HEAD `17b0cd6` (2026-08-23 00:48) · `main` เท่ากับ `origin/main` · ไม่มีอะไรค้าง

**หน้าจริง 47 หน้า** — อังกฤษ 24 ไทย 23 · **ทางเบี่ยง 26 ไฟล์** เป็น meta refresh + `noindex` ชี้ไปชื่อใหม่

หน้าเดียวที่ไม่มีคู่ภาษาคือ `long-read-museums-and-libraries.html` ฝั่งไทยไม่มี

**`python3 .tools/check.py`** → 74 ไฟล์ · ไม่มี error · เตือน 24 ข้อ เป็นตัวเลขในบทความกาแฟที่มีแหล่งอ้างอิงกำกับ ไม่ต้องแก้

### ขึ้นเว็บแล้ว — งานรอบ 08-21

- `seo-as-knowledge-management.html` + ฉบับไทย พร้อมเครื่องมือ `build_evidence.py` · `copy_evidence.py`
- ลิงก์เมนู How we built this site / เว็บนี้สร้างมาอย่างไร ครบทุกหน้า
- `LESSONS.md` 9 → 10 ข้อ · `sitemap.xml` · `build_jsonld.py`
- `.gitignore` เพิ่ม `node_modules/` — 26 MB ไม่ขึ้นรีโปแล้ว ส่วน `package.json` กับ `package-lock.json` เก็บไว้
  `npm install` แล้วได้ `jsdom` เวอร์ชันเดิม ซึ่งเป็นตัวที่ `split_css.mjs` ใช้

ตรวจหน้าใหม่ตามกฎข้อ 10 แล้ว ผ่านทั้งชุด — JSON-LD เหลือก้อนเดียว · ไม่เหลือ breadcrumb ของแม่แบบ · `aria-current` ชี้มาที่ตัวเอง

### ขึ้นเว็บแล้ว — งานรอบ 08-22 ถึง 08-23 (สี่ก้อน)

`.tools/add_evidence_footer.py`

เดิมหน้า SEO มีทางเข้าจากเมนูสไลด์อย่างเดียว แต่ปุ่มขีดสามขีดโผล่เฉพาะจอแคบกว่า 1300px
จอกว้างกว่านั้นจึงเข้าหน้านี้ไม่ได้เลย ไม่มีทั้งในแถบเมนูบน ในสามประตูหน้าแรก และใน footer
ตรวจแล้วเป็นหน้าเดียวในเว็บที่เป็นแบบนี้ จึงเติมลงคอลัมน์สุดท้ายของ footer ต่อจาก เราคือใคร
ให้ตรงลำดับกับในเมนู และใช้ป้ายคำเดียวกัน ไม่ได้ตั้งชื่อใหม่

ยืนยันด้วย jsdom ที่โหลด `assets/site.en.css` และ `site.th.css` เข้าไปเอง แล้ววัด `getComputedStyle`
ทั้งสองภาษาได้ `display:block` กับ `color:var(--dim)` ตรงกับลิงก์อื่นใน footer

> **กับดักตอนวัดด้วย jsdom** — CSS ของเว็บนี้อยู่ในไฟล์ภายนอกต่อภาษาตั้งแต่ `ca9f745`
> jsdom ไม่ไปดึงให้ ถ้าไม่แทรกเข้าไปเองจะอ่านได้เป็นค่า default ของเบราว์เซอร์
> (`display:inline` · `color:rgb(0,0,238)`) แล้วเข้าใจผิดว่า CSS ไม่ติด

`.tools/add_nav_dropdown.py`

เมนูย่อยบนแถบเมนูบน สำหรับจอกว้างกว่า 1300px ตามกติกา 9b ข้างล่าง
สี่หัวข้อได้เมนูย่อย — แนวคิด (4) · MKM พิพิธภัณฑ์และห้องสมุด (9) · MKM กาแฟ (5) · เราคือใคร (2)
Engagement ไม่ได้ เพราะไม่ใช่หัวของกลุ่มไหนในเมนูสไลด์

**รายการในเมนูย่อยอ่านจากเมนูสไลด์ในไฟล์เดียวกัน ไม่ได้เขียนขึ้นใหม่** หลักเดียวกับ
`add_breadcrumbs.py` ที่ดึงเส้นทางจาก JSON-LD ของหน้านั้น สองที่จึงตรงกันโดยโครงสร้าง
กฎจับคู่คือ ลิงก์แรกของกลุ่มในเมนูสไลด์ต้องชี้ไปหน้าเดียวกับหัวข้อบนแถบบน

เปิดด้วยชี้ค้างและ `:focus-within` จึงไม่ต้องใช้ JS มีสคริปต์บรรทัดเดียวในเปลือกเมนูไว้รับ Escape
หัวข้อยังเป็นลิงก์ไปหน้าตัวเองเหมือนเดิม ไม่ได้กลายเป็นปุ่มเปล่า

วัดด้วย jsdom + `getComputedStyle` ทุกหน้า — ตั้งต้นซ่อน · `position:absolute` · `z-index` 120 ·
ลิงก์ในแผงเป็น `display:block` (ชนะกฎ `.nav-links a` ด้วย specificity ไม่ใช่ด้วยลำดับ) ·
ไทยได้ `line-height:1.85`

`.tools/split_coffee.py` + `.tools/coffee-source/`

บทความกาแฟ 4,436 คำ (ไทย 22,000 ตัวอักษร) แบ่งเป็นสามภาคตามเส้นเหตุผล ทำไม · อย่างไร · แล้วไงต่อ

```
ภาค 1  s01–s03  mkm-for-coffee.html            URL เดิม ไม่เปลี่ยน · แผนที่ Coffee Belt อยู่ที่นี่
ภาค 2  s04–s07  mkm-for-coffee-why-now.html    รูปประกอบสองชิ้นตามมาที่นี่
ภาค 3  s08–s10  mkm-for-coffee-commons.html    รายการแหล่งอ้างอิงตามมาที่นี่
```

**เนื้อหาไม่ถูกแตะเลยแม้แต่ตัวเดียว** ตรวจโดยดึงข้อความที่มองเห็นในเขต `.artbody` ของทั้งสามภาค
มาต่อกัน แล้วเทียบกับต้นฉบับตัวต่อตัว — อังกฤษ 25,485 ตรงกัน ไทย 21,795 ตรงกัน
คำใหม่ที่จำเป็น (title · ป้ายภาค · ป้ายปุ่มก่อนหน้า-ถัดไป) ยกมาจากหัวข้อ h2 ของเขาเองทั้งหมด
`meta description` ตัดที่ขอบวรรคของย่อหน้าแรก หลักเดียวกับ `fix_meta.py` คือไม่เขียนใหม่

สารบัญยังแสดงครบสิบหัวข้อทุกภาค หัวข้อที่อยู่คนละภาคชี้ข้ามหน้าไป ฟอร์มสมัครอยู่ครบทั้งสามภาค
`topic:'MKM Coffee'` ในฟอร์ม **ห้ามเปลี่ยน** Worker มี allow-list รับแค่ค่านี้ค่าเดียว
ตัวแยกว่าคนกรอกมาจากภาคไหนคือ `page:location.pathname` ที่ส่งไปอยู่แล้ว

---

## เครื่องมือใน `.tools/` — ตัวไหนเป็นเจ้าของเรื่องอะไร

**แก้ที่เครื่องมือแล้วรันใหม่ อย่าแก้ HTML ทีละไฟล์**

| เครื่องมือ | เป็นเจ้าของ |
|---|---|
| `check.py` | ด่านตรวจก่อน publish · รู้ว่ามันตรวจอะไรก่อนแก้อะไร |
| `shell/nav.en.html` · `shell/nav.th.html` | ต้นฉบับเมนู ภาษาละหนึ่งชุด |
| `sync_nav.py` | กระจายเมนูลงทุกหน้า · ใส่ปุ่มสลับภาษาและ `aria-current` ให้เอง |
| `build_jsonld.py` | กราฟ JSON-LD ทั้งเว็บ · `DefinedTerm` · `Service` · `BreadcrumbList` |
| `add_breadcrumbs.py` | แถบเส้นทางที่คนเห็น · **ดึงจาก JSON-LD ของหน้านั้นเอง** สองอย่างจึงตรงกันโดยโครงสร้าง |
| `build_gates.py` | สามประตูในหน้าแรก |
| ~~`build_coffee.py`~~ · ~~`make_coffee_belt.py`~~ | **รันไม่ได้แล้ว** ดูหัวข้อข้างล่าง |
| `split_coffee.py` | แบ่งบทความกาแฟเป็นสามภาค · ต้นฉบับอยู่ `.tools/coffee-source/` |
| `build_farmer_page.py` | หน้าเกษตรกร |
| `build_privacy.py` | หน้าความเป็นส่วนตัว |
| `build_evidence.py` + `copy_evidence.py` | หน้า SEO · เนื้อหาอยู่ในไฟล์ที่สอง |
| `split_css.mjs` | แยก CSS ต่อภาษา (ใช้ jsdom) |
| `add_evidence_footer.py` | ลิงก์หน้า SEO ใน footer · รันซ้ำได้ · ข้ามบทความยาวโดยตั้งใจ |
| `add_nav_dropdown.py` | เมนูย่อยบนแถบเมนูบน · อ่านรายการจากเมนูสไลด์ · เรียก `sync_nav.py` ต่อให้เอง |

ที่เหลือเป็นสคริปต์ใช้ครั้งเดียวจบ ขึ้นต้นด้วย `fix_` `add_` `rename_` `move_` `drop_` `reorder_` `relayout_` `dedupe_` `verify_`
เก็บไว้เป็นบันทึกว่าเคยแก้อะไรไปด้วยกฎข้อไหน ไม่ต้องรันซ้ำ

`regroup_nav.py` ถูกแทนที่ด้วย `sync_nav.py` แล้ว อย่าใช้

### บทความกาแฟ — เครื่องมือเดิมตายแล้ว ต้นฉบับอยู่ที่ใหม่

`build_coffee.py` กับ `make_coffee_belt.py` **รันไม่ได้อีกแล้ว** ทั้งคู่ฮาร์ดโค้ดพาธไปที่
sandbox ที่ไม่มีอยู่แล้ว และไฟล์บทความต้นทาง (`article-modern-km-for-coffee*.html`)
ไม่ได้อยู่ในรีโป ถึงกู้มาได้ การรันใหม่ก็จะลบงานที่แก้มาหลังจากนั้นทิ้งทั้งหมด —
JSON-LD · breadcrumb · footer สี่คอลัมน์ · เมนูย่อย · `skip` link ล้วนไม่มีในแม่แบบของมัน
เก็บไว้อ่านเป็นบันทึกว่าเคยสร้างมาอย่างไรเท่านั้น

**ต้นฉบับบทความฉบับเต็ม ก่อนแบ่งภาค อยู่ที่**

```
.tools/coffee-source/mkm-for-coffee.html.src
.tools/coffee-source/th-mkm-for-coffee.html.src
```

แก้เนื้อหาที่นั่น แล้วรัน `split_coffee.py` ใหม่ สามภาคจะถูกสร้างทับให้เอง

นามสกุล `.html.src` ตั้งใจ — `.tools/` ทั้งโฟลเดอร์ถูกเสิร์ฟออกเว็บ ถ้าเก็บเป็น `.html`
จะกลายเป็นบทความฉบับเต็มอีกชุดที่บอตเปิดได้ แล้วไปแย่งอันดับกับสามภาคที่เป็นของจริง

> ⚠️ เรื่องที่ยังไม่ตัดสิน — `robots.txt` ตอนนี้เป็น `Allow: /` ทั้งเว็บ แปลว่า
> `.tools/*.py` ทุกไฟล์ถูกจัดทำดัชนีได้ ไม่ใช่ความลับ แต่ก็ไม่ใช่เนื้อหาที่ตั้งใจให้ค้นเจอ
> จะเติม `Disallow: /.tools/` ไหม เป็นเรื่องที่ Noppadol ต้องตัดสิน

### CSS อยู่ที่ไหน — ไม่สมมาตร ต้องรู้ก่อนเพิ่มกฎใหม่

`split_css.mjs` ถอดออกไฟล์ร่วมได้เฉพาะกฎที่ซ้ำกันตั้งแต่ 80% ของหน้าในภาษานั้น ผลคือ

```
ฝั่งไทย   assets/site.th.css  30 KB  หน้าเหลือ inline 0–3 KB
ฝั่งอังกฤษ assets/site.en.css   4 KB  หน้ายังเก็บ inline ไว้ 19–31 KB ต่อหน้า
```

และมี **11 หน้าที่ไม่ได้ลิงก์ไฟล์ร่วมเลย** เก็บ CSS ไว้ในตัวเองทั้งหมด
(`coffee-demo` `coffee-farmer` `mkm-for-coffee` `privacy` `seo-as-knowledge-management`
`long-read-museums-and-libraries` และคู่ไทยของสี่ตัวแรก)

**กฎที่ตามมา สำหรับกฎ CSS ใหม่ที่ต้องมีผลทั้งเว็บ**

1. ใส่ในไฟล์ร่วมทั้งสอง **และ** ฝังในหน้าที่ไม่ได้ลิงก์ ทั้งสองอย่าง
2. ตั้ง specificity ให้สูงกว่ากฎที่จะไปชนใน inline เช่นใช้ `.nav-links .sub a` ไม่ใช่ `.sub a`
   เพราะบล็อก inline อยู่หลังไฟล์ร่วม ถ้า specificity เท่ากัน inline ชนะ
3. แทรกที่ `</style>` ตัวท้ายที่ยังอยู่ใน `<head>` **ไม่ใช่ตัวท้ายของไฟล์**
   บทความยาวมี `<style>` อีกก้อนซ้อนอยู่ใน `<svg><defs>` ซึ่งเป็นคนละ namespace
   กฎที่ตกในนั้นไม่มีผลกับหน้า — เจอจริงตอนวัดด้วย jsdom ไม่ใช่ตอนอ่านโค้ด
4. **ห้ามตัด CSS ด้วย regex ถ้ากฎนั้นอาจซ้อนชั้น** `@media` ซ้อนอีกชั้น regex จึงตัดพลาด
   ให้ไล่นับปีกกาแทน ดู `rules_of()` ใน `split_coffee.py` และดูข้อ 11 ของ `LESSONS.md`
   ว่าความพลาดนี้หน้าตาเป็นอย่างไรตอนขึ้นเว็บจริง
   `check.py` มีด่านนับปีกกาในบล็อก `<style>` ทุกหน้าแล้ว ตั้งแต่ 08-23

### ลำดับที่ห้ามสลับ

หน้าใหม่ที่สร้างจากแม่แบบ ต้องล้างคำประกาศของแม่แบบก่อน แล้วให้เจ้าของเรื่องใส่กลับ

```
สร้างหน้าจากแม่แบบ → ลบ JSON-LD · breadcrumb · aria-current ของแม่แบบทิ้ง
                   → sync_nav.py → build_jsonld.py → add_breadcrumbs.py
```

`build_evidence.py` เรียกสองตัวแรกในตัวเองแล้ว ไม่ต้องจำเอง
เหตุผลอยู่ในข้อ 10 ของ `LESSONS.md` ซึ่งเกิดจากหน้านี้พอดี

---

## สิ่งที่ตัดสินไปแล้ว — ห้ามรื้อใหม่

1. โครงสามภาค ไม่ปนกัน เชื่อมโยงกันได้
2. MKM for Coffee เป็น practice area ที่สอง หน้าตากลืนธีมเว็บ มีฉบับไทยจริง
3. **ไม่ใส่ราคาหรือช่วงราคาบนเว็บ** เพราะเป็นงานที่ปรึกษา
4. copy ใหม่ Claude เขียนแล้วขึ้นได้เลย ไม่ต้องรออนุมัติทีละชิ้น
5. PDPA ไม่ลงรายละเอียดกฎหมาย ยืนยันแค่หลัก AI Sovereignty
6. แผนที่ Coffee Belt ไม่มาร์กประเทศ มีแค่แถบเขตร้อน
7. deploy Worker ผ่าน `deploy.command` ไม่ใช่ให้ Claude รัน wrangler
8. หน้าที่ไม่อยู่ในโครงสามภาค **ไม่มี BreadcrumbList โดยตั้งใจ** เพราะไม่มีเส้นทางให้แสดง ไม่ใช่ของหลุด
   มี 5 หน้าต่อภาษา — `index` `about` `contact` `privacy` `seo-as-knowledge-management`
   หน้าอื่นมีครบทุกหน้า เจอหน้าไหนที่ควรมีแล้วไม่มี แปลว่าลืมรัน `add_breadcrumbs.py`
   ระวังตอนตรวจ หน้า SEO พูดถึงคำว่า BreadcrumbList ในเนื้อหาด้วย `grep` หาคำนี้จึงเจอทั้งที่ไม่มีโหนดจริง ต้องอ่านจาก `@graph`
9. เปลี่ยนชื่อหน้าแล้วต้องทิ้งทางเบี่ยงไว้เสมอ ลิงก์เก่าที่คนอื่นแปะไว้ต้องไม่ตาย
9b. **ทุกหน้าต้องมีทางเข้าที่ใช้ได้บนจอกว้าง** เมนูสไลด์ไม่นับ เพราะปุ่มเปิดมันโผล่เฉพาะจอแคบกว่า 1300px
   ทางเข้าที่นับได้คือแถบเมนูบน สามประตูหน้าแรก หรือ footer อย่างน้อยหนึ่งอย่าง
   สร้างหน้าใหม่เมื่อไร ให้เช็กข้อนี้ก่อนบอกว่าเสร็จ
9c. บทความยาวใช้ footer แบบแถบเดียวห้าลิงก์ ไม่มีคอลัมน์ **ไม่เติมลิงก์เข้าไปโดยไม่ถามก่อน**
   `add_evidence_footer.py` จึงข้ามหน้านั้นไว้ในตัวแปร `EXCEPT` ไม่ใช่ข้ามเงียบ ๆ
10. **ความเห็นรีวิวชุดที่พูดถึง Data Science / AI Transformation / ทีมงานรุ่นใหม่ ไม่ได้อ่านเว็บจริง** ตรวจแล้วคำเหล่านั้นอยู่ในเว็บ 0 ไฟล์ ห้ามเอาไปแก้ตาม

---

## งานค้าง

**ปิดแล้วเมื่อ 08-23 — Noppadol ตัดสินครบทั้งสี่ข้อ**

1. หน้าแรก ย่อหน้า *"We start with museums, libraries and archives…"* → **คงไว้** ไม่ต้องแตะ
2. อีเมล → **`hello@` ทั้งเว็บ** · ตรวจแล้ว `noppadol@neogens.co` มี **0 จุด** ในไฟล์เว็บอยู่แล้ว ไม่ต้องแก้อะไร
   ข้อนี้ในฉบับก่อนตกยุค commit `37c98b8` ถูกกลับทางไปก่อนหน้านี้แล้ว
3. คำว่า **ครับ** ในเนื้อความ → **ตัดออกครบ 10 จุด** ใน 4 หน้า รวมที่ไหลไปอยู่ใน `meta description` `og:description` และ JSON-LD

   ```
   th-services.html                      4 จุด   "…ที่ปรึกษาและออกแบบครับ ไม่ใช่คนขายระบบ"
   th-what-we-wont-do.html               4 จุด   "…มารู้เรื่องนี้เอาตอนเดือนที่สี่ครับ"
   th-engagement.html                    1 จุด   "นั่นแหละครับคือสิ่งที่ส่งมอบ"
   th-mkm-for-museums-and-libraries.html 2 จุด   "ลองไล่ดูทีละขั้นครับ" · "วินัยเป็นเรื่องเดียวกันครับ"
   ```

   > **ฉบับก่อนนับผิด** เขียนว่า th-what-we-wont-do มี 3 จุด ของจริงมี 4 — `og:description` ตกสำรวจ
   > บทสนทนาจำลองหน้าละ 2 จุด (ตลิ่งแม่น้ำ · ข้อเถียง) ไม่ได้แตะ ถูกกติกาอยู่แล้ว
   > ด่านตรวจหลังแก้ใช้เงื่อนไข ทุกหน้าไทยต้องมี `ครับ` เป็น 0 หรือ 2 เท่านั้น

4. ลิงก์ long-read ฝั่งไทย → **เติม `(อังกฤษ)` เฉพาะใน footer 23 หน้า** แถบเมนูบนกับเมนูสไลด์คงป้ายเดิม เพราะป้ายจะยาวเกิน

   > **ฉบับก่อนนับผิดอีกข้อ** เขียนว่า *ลิงก์ฝั่งไทยสามจุด* ของจริงคือ **สามที่ต่อหน้า × 23 หน้า = 69 จุด**
   > บวกอีกสองจุดที่กำกับอยู่แล้ว คือสามประตูใน `th-index.html` กับการ์ดใน `th-reference-implementation.html`
   > แถบเมนูบนกับเมนูสไลด์มีต้นฉบับเดียวที่ `.tools/shell/nav.th.html` แก้ที่นั่นแล้วรัน `sync_nav.py`
   > ส่วน footer ไม่มีต้นฉบับ ต้องไล่ทีละหน้า

**ยังยืนยันไม่ได้ในเซสชันนี้**

- Cloudflare Worker อยู่คนละรีโป (`~/projects/neogens-briefing-worker`) เซสชันนี้ไม่ได้ต่อโฟลเดอร์นั้น
  บันทึกเดิม 08-15 ระบุว่า **รอบที่แก้ `RATE_LIMIT_PER_HOUR` 5 → 20 ยังไม่ได้ deploy** ต้องเปิดรีโปนั้นดูก่อนถึงจะยืนยันได้
- ฟอร์มในหน้าอื่นนอกจากหน้ากาแฟ ยังไม่แยกข้อความกรณีโดน 429 ออกจากกรณีล้มเหลวจริง ถ้าโดนจะขึ้นว่าส่งไม่สำเร็จเฉย ๆ ซึ่งบอกสาเหตุไม่ตรง
- `https://www.neogens.co/` (ราก) เคยคืนหน้าเก่าก่อน rename มาให้ตอนดึงเมื่อ 08-22 ขณะที่ `/index.html` เป็นฉบับปัจจุบัน
  น่าจะเป็นแคชที่ราก แต่ยืนยันแทนเบราว์เซอร์ไม่ได้ ให้เปิดของจริงแล้วดูว่าเมนูบนสุดขึ้น `MKM for Coffee` ไหม

---

## Constraints

**Hard**

- **อ่าน `.tools/LESSONS.md` ให้จบก่อนพิมพ์ตัวอักษรแรกของเซสชัน** แล้วขึ้นต้นข้อความแรกด้วย `อ่าน .tools/LESSONS.md แล้ว — N ข้อ` ตอนนี้ N คือ 15
- **ข้อความที่ Noppadol เขียนมาเอง ห้ามแก้แม้แต่ตัวเดียว** รวมทั้งตัวสะกดที่ดูเหมือนพิมพ์ผิด เห็นอะไรที่ควรแก้ ให้ถามก่อนแล้วรอคำตอบ
- ใช้ `git --no-optional-locks` กับ `status` และ `diff` เสมอ และเช็กว่า `.git/index.lock` ไม่ค้างก่อนบอกว่างานเสร็จ
- Claude แก้ไฟล์อย่างเดียว ไม่ commit ไม่ push ไม่ deploy เอง
- แก้ copy ที่ไหน ต้องแก้อีกภาษาให้ตรงกัน และเช็ก `<title>` `meta description` `og:description` `alt` `aria-label` `placeholder`
- ห้าม letter-spacing ติดลบในกฎ CSS ที่มีข้อความไทย · line-height ไทยอยู่ที่ 1.75–1.9
- ห้ามใส่ตัวเลขสถานะระบบที่ยืนยันไม่ได้ · ห้ามใส่ชื่อฐานข้อมูล เฟรมเวิร์ก หรือสถาปัตยกรรมภายในบนเว็บ
- หน้าไหนอ้างตัวเลขในเนื้อหา ให้สคริปต์นับจากไฟล์จริงทุกครั้งที่รัน แล้วหยุดถ้าไม่ตรง ดู `FACTS` กับ `live_facts()` ใน `build_evidence.py`

**Soft**

- ภาษาไทยของ Neo Gens ใช้สกิล `neogens-thai-voice` เมื่อ Claude เป็นคนเขียน · ห้ามลงท้าย ครับ ในเนื้อความ · เขียนจากความคิด ไม่แปลจากอังกฤษ
- เนื้อหาใหม่ให้สั้น กระชับ เข้าประเด็น
- แก้ทั้งไซต์ด้วยสคริปต์ที่เก็บไว้ใน `.tools/` แล้วตรวจด้วย diff ระดับข้อความที่มองเห็น
- ด่านตรวจทุกชุดต้องมีทั้งข้อ *มีครบ* และข้อ *ไม่เหลือ*
- ประหยัด context — กรองผลก่อนดึงเข้ามา อย่าเปิดไฟล์ HTML ทั้งไฟล์ (ไฟล์ละ 27–186 KB)

---

## Context files — อ่านตามลำดับนี้

1. `.tools/LESSONS.md` — 15 ข้อ **บังคับอ่านก่อนพิมพ์อะไรทั้งสิ้น**
2. `CLAUDE.md` — กติกาโปรเจกต์
3. `.tools/HANDOFF.md` — ไฟล์นี้
4. `.tools/check.py` — รู้ว่ามันตรวจอะไรก่อนแก้อะไร
5. `.tools/shell/nav.en.html` · `nav.th.html` + `sync_nav.py` — โครงเมนูที่ใช้จริง
6. `.tools/build_jsonld.py` — กราฟความรู้ของเว็บ อ่านก่อนแตะ structured data
7. `.tools/build_evidence.py` — ตัวอย่างสคริปต์สร้างหน้าที่ทำถูกตามข้อ 10 ใช้เป็นแม่แบบได้
8. `.tools/build_coffee.py` · `make_coffee_belt.py` — อ่านเฉพาะเมื่อจะแก้หน้ากาแฟหรือแผนที่
9. `~/projects/neogens-briefing-worker/src/index.js` + `test_worker.mjs` — อ่านเฉพาะเมื่อจะแตะฟอร์มหรือ Worker

---

## สิ่งที่ Noppadol ต้องกดเอง

1. `check.command` แล้ว `publish.command` ที่รากรีโป
   ขั้น 3/5 ของ `publish.sh` จะโชว์รายการไฟล์ที่กำลังจะขึ้น **อ่านให้จบก่อนตอบ y** เจอไฟล์ที่ไม่ได้ตั้งใจแตะให้ตอบ N แล้วมัน `git reset` คืนให้เอง
2. เปิดหน้าใหม่ดูด้วยตา ทั้งธีมมืดและสว่าง สองความกว้าง — **Claude เรนเดอร์หน้าเว็บดูเองไม่ได้ในสภาพแวดล้อมนี้** ยืนยันได้แค่โครงสร้างกับเนื้อหา ยืนยันไม่ได้ว่าหน้าตาสวย
3. **ฟอร์มทดสอบได้เฉพาะบน `https://www.neogens.co/...` เท่านั้น** Worker ยอมรับเฉพาะ Origin `neogens.co` และ `www.neogens.co` เปิดด้วย `file://` จะได้ CORS error เสมอ เคยเสียเวลาไล่หาสาเหตุผิดมาแล้วหนึ่งรอบ ดูข้อ 7 ใน `LESSONS.md`
4. `~/projects/neogens-briefing-worker/deploy.command` ถ้ายืนยันแล้วว่ารอบ rate limit ยังไม่ได้ deploy

---

## สั่งงานจากมือถือระหว่างเดินทาง

`remote.command` ที่รากรีโป — ดับเบิลคลิกแล้วปล่อยหน้าต่างเปิดค้างไว้ เซสชันรันบนเครื่องนี้
มือถือเป็นแค่หน้าต่างเข้ามาสั่ง เปิดแอป Claude แตะแท็บ Code แล้วเลือก **Neo Gens website**

- ใช้โหมด interactive ไม่ใช่ server mode เพราะ server mode เลิกเองหลังเน็ตหลุดราวสิบนาที
- มี `caffeinate` กันเครื่องหลับ และ loop เปิดใหม่ให้เองถ้าโปรแกรมหลุด
- **แจ้งเตือนบนมือถือต้องเปิดตามลำดับ** เปิดเซสชัน แล้วเชื่อมมือถือให้ติดก่อน จากนั้นค่อยพิมพ์ `/config` บนเครื่อง สองตัวเลือก `Push when Claude decides` กับ `Push when actions required` จะโผล่ก็ต่อเมื่อ Remote Control ทำงานและมือถือลงทะเบียนแล้ว ถ้าขึ้น `No mobile registered` ให้เปิดแอปบนมือถือค้างไว้แล้วรอเชื่อมรอบใหม่
- ปิดหน้าต่าง Terminal เมื่อไร เซสชันออฟไลน์ทันที
- Claude ยังไม่ commit ไม่ push ตามกติกาเดิม ถ้าต้องการให้ deploy แทนระหว่างเดินทาง ต้องสั่งให้ชัดในเซสชันนั้น
